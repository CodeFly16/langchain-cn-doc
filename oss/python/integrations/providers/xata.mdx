---
title: "Xata 集成"
description: "使用 LangChain Python 与 Xata 集成。"
---

> [Xata](https://xata.io) is a serverless data platform, based on `PostgreSQL`.
> 它提供 a Python SDK for interacting with your database, and a UI
> for managing your data.
> `Xata` has a native vector type, which can be added to any table, and
> supports similarity search. LangChain inserts vectors directly to `Xata`,
> and queries it for the nearest neighbors of a given vector, so that you can
> use all the LangChain Embeddings integrations with `Xata`.


## 安装和设置


我们需要安装 `xata` Python 包.

<CodeGroup>
```bash pip
pip install xata==1.0.0a7
```

```bash uv
uv add "xata==1.0.0a7"
```
</CodeGroup>

## 向量存储

查看[使用示例](/oss/python/integrations/vectorstores/xata).

```python
from langchain_community.vectorstores import XataVectorStore
```

---

<div className="source-links">
<Callout icon="terminal-2">
    [将这些文档连接](/use-these-docs) 到 Claude、VSCode 等工具，通过 MCP 获取实时答案。
</Callout>
<Callout icon="edit">
    [在 GitHub 上编辑此页面](https://github.com/langchain-ai/docs/edit/main/src/oss/python/integrations/providers/xata.mdx)或[提交 issue](https://github.com/langchain-ai/docs/issues/new/choose).
</Callout>
</div>
