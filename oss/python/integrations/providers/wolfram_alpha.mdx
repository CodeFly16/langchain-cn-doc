---
title: "Wolfram Alpha 集成"
description: "使用 LangChain Python 与 Wolfram Alpha 集成。"
---

>[WolframAlpha](https://en.wikipedia.org/wiki/WolframAlpha) is an answer engine developed by `Wolfram Research`.
> It answers factual queries by computing answers from externally sourced data.

This page covers how to use the `Wolfram Alpha API` within LangChain.

## 安装和设置
- Install requirements with
<CodeGroup>
```bash pip
pip install wolframalpha
```

```bash uv
uv add wolframalpha
```
</CodeGroup>

- Sign up for a [Wolfram Alpha developer account](https://developer.wolframalpha.com/)
- Create an app and get your `APP ID`
- Set your APP ID as an environment variable `WOLFRAM_ALPHA_APPID`

## Wrappers

### 工具类

There exists a WolframAlphaAPIWrapper utility which wraps this API. To import this utility:

```python
from langchain_community.utilities.wolfram_alpha import WolframAlphaAPIWrapper
```

For a more detailed walkthrough of this 封装器的更详细演练，请参阅[此笔记本](/oss/python/integrations/tools/wolfram_alpha).

### 工具

您也可以轻松地将此封装器加载为工具（与智能体一起使用）。
您可以通过以下方式完成：
```python
from langchain_community.agent_toolkits.load_tools import load_tools
tools = load_tools(["wolfram-alpha"])
```

有关工具的更多信息，请参阅 [Tools](/oss/python/integrations/tools).

---

<div className="source-links">
<Callout icon="terminal-2">
    [将这些文档连接](/use-these-docs) 到 Claude、VSCode 等工具，通过 MCP 获取实时答案。
</Callout>
<Callout icon="edit">
    [在 GitHub 上编辑此页面](https://github.com/langchain-ai/docs/edit/main/src/oss/python/integrations/providers/wolfram_alpha.mdx)或[提交 issue](https://github.com/langchain-ai/docs/issues/new/choose).
</Callout>
</div>
