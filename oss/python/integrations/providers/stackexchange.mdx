---
title: "Stack Exchange 集成"
description: "使用 LangChain Python 与 Stack Exchange 集成。"
---

>[Stack Exchange](https://en.wikipedia.org/wiki/Stack_Exchange) is a network of
question-and-answer (Q&A) websites on topics in diverse fields, each site covering
a specific topic, where questions, answers, and users are subject to a reputation award process.

This page covers how to use the `Stack Exchange API` within LangChain.

## 安装和设置
- Install requirements with
<CodeGroup>
```bash pip
pip install stackapi
```

```bash uv
uv add stackapi
```
</CodeGroup>

## Wrappers

### 工具类

There exists a StackExchangeAPIWrapper utility which wraps this API. To import this utility:

```python
from langchain_community.utilities import StackExchangeAPIWrapper
```

For a more detailed walkthrough of this 封装器的更详细演练，请参阅[此笔记本](/oss/python/integrations/tools/stackexchange).

### 工具

您也可以轻松地将此封装器加载为工具（与智能体一起使用）。
您可以通过以下方式完成：
```python
from langchain_community.agent_toolkits.load_tools import load_tools
tools = load_tools(["stackexchange"])
```

有关工具的更多信息，请参阅 [Tools](/oss/python/integrations/tools).

---

<div className="source-links">
<Callout icon="terminal-2">
    [将这些文档连接](/use-these-docs) 到 Claude、VSCode 等工具，通过 MCP 获取实时答案。
</Callout>
<Callout icon="edit">
    [在 GitHub 上编辑此页面](https://github.com/langchain-ai/docs/edit/main/src/oss/python/integrations/providers/stackexchange.mdx)或[提交 issue](https://github.com/langchain-ai/docs/issues/new/choose).
</Callout>
</div>
